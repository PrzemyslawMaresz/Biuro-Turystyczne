import { CurrencyConventerPipe } from './currency-conventer.pipe';

describe('CurrencyConventerPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencyConventerPipe();
    expect(pipe).toBeTruthy();
  });
});
